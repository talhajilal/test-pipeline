pipeline {
    agent any 
    stages {
        stage('docker-test') {
            steps {
                echo 'Hello world!' 
            }
        }
pipeline = load 'first.groovy'
        pipeline.functionA()
    }
    pipeline.functionB()
    }

