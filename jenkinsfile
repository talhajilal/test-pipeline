pipeline {
    agent any 
    stages {
        stage('docker-test') {
            steps {
                echo 'Hello world!' 
            }
        }
        {
        pipeline = load 'pipeline.groovy'
        pipeline.functionA()
    }
    pipeline.functionB()
    }
}
